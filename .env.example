# Dev: client uses same-origin + Vite proxy (leave as backend target only).
# Do not point the browser at :3001 directly — that triggers CORS on :5175.
VITE_API_URL=http://127.0.0.1:3001
VITE_ADMIN_REACT_URL=http://localhost:5174
VITE_ADMIN_VUE_URL=http://localhost:5175
VITE_APP_NAME=SmartБотаник Admin (Vue)
